# Lokale Bibliotheken

`android-smartview-sdk-2.5.34.aar` enthält das bereitgestellte Samsung Smart View SDK als lokale AAR.
Die App bindet diese Datei direkt in `app/build.gradle.kts` ein. Es gibt keinen Gradle-Installer,
keinen SDK-Downloadtask und keine Suche in lokalen Download-Ordnern.
